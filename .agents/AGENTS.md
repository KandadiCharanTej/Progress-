<!-- BEGIN:layout-rule -->
# Layout Constraints
All study curriculum and dashboard pages must use a horizontal `h-full grid` layout that fits perfectly on one screen without vertical window scrolling. 
You must structure inner content using `flex-1 overflow-y-auto no-scrollbar` to ensure no boxes or cards are ever cut off by the window boundary.
<!-- END:layout-rule -->
